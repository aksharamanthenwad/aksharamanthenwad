#include<stdio.h>
main()
{
	int temperature;
	printf("enter the temperature");
	scanf("%d",&temperature);
	if(temperature<0)
	{
		printf("freezing\n");
	}
	if else(temperature>=0&&temperature<=20)
	{
		printf("cold\n");
	}
	else if(temperature>=21&&temperature<=20)
	{
		printf("moderate\n");
	}
}
